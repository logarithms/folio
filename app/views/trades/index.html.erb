<h1>Listing trades</h1>

<table>
  <tr>
    <th>Ameritradeid</th>
    <th>Symbol</th>
    <th>Date</th>
    <th>Qty</th>
    <th>Amount</th>
    <th>Action cd</th>
    <th>State cd</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @trades.each do |trade| %>
  <tr>
    <td><%= trade.ameritradeid %></td>
    <td><%= trade.symbol %></td>
    <td><%= trade.date %></td>
    <td><%= trade.qty %></td>
    <td><%= trade.amount %></td>
    <td><%= trade.action_cd %></td>
    <td><%= trade.state_cd %></td>
    <td><%= link_to 'Show', trade %></td>
    <td><%= link_to 'Edit', edit_trade_path(trade) %></td>
    <td><%= link_to 'Destroy', trade, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Trade', new_trade_path %>
