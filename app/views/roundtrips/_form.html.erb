<%= form_for(@roundtrip) do |f| %>
  <% if @roundtrip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roundtrip.errors.count, "error") %> prohibited this roundtrip from being saved:</h2>

      <ul>
      <% @roundtrip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :open_id %><br />
    <%= f.text_field :open_id %>
  </div>
  <div class="field">
    <%= f.label :close_id %><br />
    <%= f.text_field :close_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
